# Environment Variables for Resume Optimization System
# Copy this file to .env and customize paths for your environment

# Scanner Configuration
RESUME_FOLDER=/path/to/your/resumes  # Default: ~/Downloads/NitinResumes
OUTPUT_FILE=./data/file_inventory.json

# Classifier Configuration  
INVENTORY_FILE=./data/file_inventory.json
CLASSIFIED_FILE=./data/classified_files.json

# Profile Database Configuration
LINKEDIN_JSON=./profile-data/linkedin-profile-parsed.json
PROFILE_JSON=./profile-data/profile-structured.json

# Report Generator Configuration
# CLASSIFIED_FILE is reused from above
REPORT_FILE=./data/classification_report.md

# Docker Usage:
# docker run -e RESUME_FOLDER=/resumes -v /local/resumes:/resumes resume-optimizer

# PyPI Package Usage:
# import os
# os.environ['RESUME_FOLDER'] = '/path/to/resumes'
# from resume_optimizer import scan_resumes
# scan_resumes()
